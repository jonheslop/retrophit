---
import { getCollection, render } from "astro:content";
import Layout from "@/components/layout.astro";

export async function getStaticPaths() {
	const posts = await getCollection("posts");
	return posts.map((post) => ({
		params: { slug: post.id },
		props: { post },
	}));
}

const { post } = Astro.props;
const { Content } = await render(post);
---

<Layout title={post.data.title}>
  <article>
    <header>
      <h1 class="post-title">
        {post.data.title}
      </h1>
      <h2 class="post-description">
        {post.data.description}
      </h2>
      <p class="post-date">
        by {post.data.author},{' '}
        {new Date(post.data.date).toLocaleDateString("en-GB",  {
          year: "numeric",
          month: "long",
          day: "numeric",
        })}
      </p>
    </header>
    <div class="content">
      <Content />
    </div>
  </article>
</Layout>

<style>
  header {
    width: 100%;
    margin: 0 0 4rem;
    padding-bottom: .75rem;
    border-bottom: 2px solid var(--ferntastic-600);
  }
  .post-title {
    font-size: 2rem;
    line-height: 1;
    margin: 0 0 2rem;
    font-weight: 700;
  }
  .post-description {
    font-size: 1.25rem;
    line-height: 1.5;
    margin: 0;
    font-weight: normal;
    font-style: italic;
  }
  .post-date {
    opacity: .6;
    font-size: 0.875rem;
    margin: .75em 0 0;
    font-family: var(--font-soehne);
  }
  .content {
    max-width: 36rem;
    margin: 0 0 8rem;

  }
  .content :global(p) {
    color: var(--tandoori-800);
    line-height: 1.5;
    margin: 0 0 1.5rem;
    text-wrap: pretty;
    font-size: 1.125rem;
  }
  .content :global(p:first-of-type) {
    text-indent: 0;
  }
  .content :global(img) {
    max-width: 100%;
    border: .5rem solid white;
    display: block;
    margin-left: -.5rem;
  }
  .content :global(.footnotes h2) {
    margin-top: 4rem;
  }
  .content :global(.footnotes ol) {
    padding-left: .75rem
  }

  @media screen and (min-width: 1024px) {
    .content :global(.footnotes ol) {
      padding-left: 0;
    }
  }

</style>
